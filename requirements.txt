jax>=0.4.26
jaxlib>=0.4.26
numpy>=1.26.0
scipy>=1.11.0
pandas>=2.1.0
pyyaml>=6.0.1
optax>=0.1.8
typing_extensions>=4.10.0
pytest>=7.4.0
fastapi>=0.110.0
uvicorn>=0.29.0
pydantic>=2.6.0
grpcio>=1.62.0
protobuf>=4.25.0
httpx>=0.27.0
defusedxml>=0.7.1

# Authentication dependencies
# Note: We use python-jose[cryptography] to ensure the cryptography backend is used
# instead of the ecdsa backend, which has known timing attack vulnerabilities.
# The [cryptography] extra ensures secure JWT operations.
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
python-multipart>=0.0.9
pyotp>=2.9.0
email-validator>=2.0.0
pytest-asyncio>=0.24.0
# python-ldap>=3.4.3  # Optional - requires system libraries (libldap2-dev, libsasl2-dev)

# Optional: FpML support (these enhance performance and validation)
# lxml>=5.0.0              # Faster XML parsing (optional)
# xmlschema>=3.0.0         # FpML schema validation (optional)
